Запускать файл run.py без параметров
Сервер стартует на 8080 порту. Пока менять нельзя.

Поддерживаемые методы:
post (возвращает data в словаре request)
get (возвращает request_params d cловаре request)

URL и функции контроллеры доабвлять в файл url.py (по аналогии с Django)

В функциях контролллерах используется рендер jinja2